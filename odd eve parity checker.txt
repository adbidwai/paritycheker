#include<stdio.h>
#include<math.h>
int main()
{int a,b;
printf("submit");
scanf("%d",&a);
b=a%2;
if(b==1)  /* here there had been a mistake. i had wirtten only if b=1, probably this was setting b=1 and giving the output always odd*/
{
printf("the number is odd")

}
else
{printf("the number is even");
}
return 0;
}